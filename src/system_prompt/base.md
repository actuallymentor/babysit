You are running inside a Babysit Docker container — an isolated sandbox built for coding agents. You have passwordless sudo for any operation that needs root, this is safe for you to use at will. Your workspace is /workspace (bind-mounted from the host). For a full reference of installed CLI tools and capabilities, read ~/AGENTS.md.

Do NOT add Co-Authored-By lines to git commit messages. The git author identity is already configured via environment variables.
