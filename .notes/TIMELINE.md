# Timeline

- **2026-08-27 (startup diagnostic formatting)**: Coordinated Mentie diagnostics with the live authentication spinner so debug, timing, warning, error, stack-trace, and multiline output clears the progress line before writing and restores it afterward. Added a real child-process regression for the shared TTY byte stream. Prepared v0.35.1.
- **2026-08-26 (workspace-aware resume history)**: Bare `babysit resume` now prefers every stored launch from the current workspace, falls back to the full registry when that workspace has no history, and accepts `--all` to show every workspace explicitly. Agent commands retain their own raw `--all` passthrough. The full Docker E2E suite passed on the local Docker host, closing the pending v0.34.0 release gate. Prepared v0.35.0.
- **2026-08-24 (high-value agent tooling)**: Added ten small Debian-backed build, process, network, filesystem, code-navigation, repository-rewrite, and PDF-structure tools to the multi-architecture image. Build gates verify public command names as the runtime user; native E2E covers real pkgconf, process, socket, ACL, inotify, formatting, history-rewrite, Ctags, and qpdf workflows. Prepared v0.34.0.
- **2026-08-23 (headful browser and PDF tooling)**: Added Xvfb, explicit X authentication, X11 diagnostics, and Poppler utilities to the multi-architecture image. Headful Puppeteer runs on demand through `xvfb-run -a`; default headless sessions retain no display dependency, Chrome's sandbox, the narrowed seccomp profile, and the existing shared-memory allowance. Native Docker coverage now drives headful Chrome and validates PDF metadata, text extraction, and rendering. Prepared v0.33.0.
- **2026-08-01 (Claude/Codex model default audit)**: Codex moved to `gpt-5.6-sol` with `model_reasoning_effort="xhigh"`, replacing GPT-5.5 after OpenAI's live resolver and model guide named GPT-5.6 Sol as the frontier model. The Codex first-run NUX suppression list now includes both `gpt-5.6` and `gpt-5.6-sol`.
- **2026-08-04 (named active sessions)**: Added optional human-readable session names, exact active-name reopening, and a `NAME` column in session listings. Names remain metadata-only and survive resume; all-digit names are reserved for numeric selectors.
- **2026-08-04 (persistent resume inventory)**: Added a zero-argument `babysit resume` history table backed by the durable Babysit session registry. It shows canonical Babysit IDs beside captured native agent IDs and sorts launches newest first. Native-id lookup now resolves repeated conversations to the latest Babysit launch. Host-native Codex/Claude histories remain excluded because their transcript stores are not mounted into Babysit's persistent agent-state volumes. Post-review hardened the dependency-check exemption to agent-less listings and made null legacy timestamps display as unknown. Released as v0.24.0.
- **2026-08-06 (active-session table readability)**: Reordered `babysit list` and numbered `babysit open` tables to show name, status, agent, ID, and tmux session in that order. Column widths now derive from visible values so long names and IDs remain aligned, while long tmux session names sit harmlessly in the final column. Released as v0.24.1.
- **2026-08-06 (global active-session numbers)**: Numbered every row in `babysit list` and made `babysit open N` resolve that global row from any directory. Bare `babysit open` still filters by the current workspace, but its multi-match table preserves the global list numbers so displayed selectors never change meaning. Released as v0.25.0.
- **2026-08-06 (release pipeline hardening)**: Added a pre-release unit-test gate, pinned Bun 1.3.13 with frozen lockfile installs and the correct cache key, and made the binary release invoke the reusable Docker workflow so versioned binaries and images deploy together. Releases are serialized with GitHub's extended pending queue, release tags point at the exact tested commit, semantic-version ordering determines the latest release, and versioned Docker tags use isolated concurrency. Nightly/asset builds update only `latest`, so rolling builds cannot displace a pending release deployment. GitHub Actions use their Node 24-compatible majors.
- **2026-08-06 (Watchtower compatibility guard)**: Researched maintained forks, popular rebuilds, registry aliases, and legacy Watchtower images. Babysit now recognizes exact repositories confirmed safe for its stateful agent containers, scans running Docker containers before launch, and emits a high-visibility warning for unknown or legacy Watchtower-like names that may ignore the exclusion label.
- **2026-08-07 (v0.26.1 release)**: Released the Watchtower registry-alias fix through the package-triggered GitHub Actions workflow. The Build & Release run completed successfully.
- **2026-08-07 (housekeeping audit)**: Revalidated agent defaults, CLI flags, pinned container tools, workflow actions, generated configuration safety, documentation, and test isolation. Claude moved to `xhigh`, Gemini to `--approval-mode=yolo`, OpenCode to `openai/gpt-5.6-sol`, sample supervision rules became opt-in, and resume tests stopped touching the user's session registry.
- **2026-08-07 (host agent profile isolation)**: Added `--ignore-host-agents-md` as a persistent session modifier. It removes shared/native global instructions and skills, executable `~/.babysitrc`, GitHub preferences, Gemini policy/UI/tracking state, and copied Claude/Codex/Gemini preferences while retaining credentials and project-local context. Independent review hardened live-resume rejection, cryptographically unique container names, cancellable create/copy, startup acknowledgment, and a typed Docker-copy plan for generated config, credential files, and secret environment bootstraps. OAuth sync now pulls/pushes through Docker's API, including auth-probe rotations and sudo-owned pull normalization; follow-up reviews made probe/startup/signal recovery retain stopped containers on flush failure, serialized sync ticks, extended source-wins conflict detection across slow Docker pulls, connected fast-exit recovery before container start, and made foreground-to-monitor ownership exclusive. Foreground recovery markers now exist before auth probes or the main container, and macOS Keychain monitors consume the transferred baseline instead of overwriting refreshed container state.
- **2026-08-18 (active-session list refresh)**: Replaced the default ID/raw-session columns with name-or-ID and compact directory fields, separated coding-agent activity from tmux attachment state, and added `babysit list --all` for full diagnostics. The initial activity display reused the monitor's idle supervision threshold. Version advanced to v0.28.0.
- **2026-08-18 (viewport activity correction)**: Corrected active-session activity so a changing tmux viewport reports `running` and one unchanged monitor interval reports `idle`, independent from configured idle-rule timeouts. Version advanced to v0.28.1.
- **2026-08-19 (active-session launch flags)**: Added an always-visible `FLAGS` column to `babysit list`, sourced from persisted session modifiers with `-` for unflagged or legacy sessions. Shared `babysit open` tables remain compact, while `list --all` retains flags alongside diagnostic IDs. Version advanced to v0.29.0.
- **2026-08-20 (prompt/startup/shutdown reliability)**: Verified OpenCode's real composer-ready invariant and added sanitized screen fixtures. Replaced configured multi-agent startup probes with active-agent checks, a 12-hour credential/image-bound success cache, cooperative Enter cancellation, non-interactive skip, and explicit `doctor --auth`. Restricted credential preflight to Claude. Traced slow close to Moby's post-process stream/logger/task cleanup and decoupled tmux foreground release through a randomized supervised-child marker while preserving detached credential recovery and container removal. Real-Docker close time fell from 4.576–4.940 seconds to 277–1,056 ms with no E2E orphans. Version advanced to v0.30.0.
- **2026-08-21 (credential-check latency diagnosis)**: Confirmed that an uncached startup check still builds a throwaway container, serially stages all supported-agent credentials, performs real inference, waits for the attached Docker cleanup tail, recovers the active credential, and removes the container before building the real session. Safe fake probes took 11.4–24.2 seconds without provider latency; one traced Codex probe spent 6.2 seconds preparing and 4.9 seconds waiting on attached exit. Ranked active-only probe mounts, batched copies, bounded asynchronous host checks, per-agent cache invalidation, and probe single-flight as behavior-preserving fixes; local status checks plus explicit deep `doctor --auth` are the fastest lower-assurance policy.
- **2026-08-21 (all-agent auth acceleration)**: Restored startup verification of Claude, Codex, Gemini, and OpenCode while retaining real model inference. Parallelized uncached probes, scoped each throwaway container to its own credentials and route-preserving profile, added a pre-capture cross-process lease with credential reconciliation, made cache identity/invalidation auth-context-aware and per-agent, bounded host preflights, and moved the timeout boundary ahead of Docker preparation. Released as v0.31.0.
- **2026-08-21 (startup regression correction)**: Replaced Codex's volatile banner/footer readiness gate with its stable composer placeholder and a visible 15-second deadline. Startup auth now checks the active agent plus supported CLIs installed on host PATH; Enter skips the whole interactive decision. OpenCode probes use the effective model and sanitized provider route in a tool-free profile, with login failures separated from route/model failures. Prepared v0.31.1.
- **2026-08-21 (bundled browser automation)**: Added current Google Chrome Stable and Puppeteer to the multi-architecture image, made daily builds invalidate the browser and coding-agent installation layers together, and configured session containers with 1 GiB shared memory plus a pinned Docker-default-derived seccomp profile. Independent review moved the profile from shared state to private per-launch transport, masked off unrelated namespaces, forced an acknowledged `docker create` boundary with generated-name-only cancellation cleanup, pinned reproducible upstream raw/canonical digests, and included Moby notices with release binaries. Verified CommonJS/ESM imports and a real sandboxed browsing flow; the amd64 image grew by about 620 MB. Prepared v0.32.0.
- **2026-08-22 (OpenCode provider-aware auth)**: Diagnosed repeated OpenCode auth failures as a provider mismatch: a healthy OpenRouter credential was being tested and launched through a hard-coded direct-OpenAI model. Unified main-launch and auth-probe model selection around explicit CLI → configured route → authenticated-provider fallback. Verified the same staged credential through both `doctor --auth opencode --refresh` and a production-style headless launch.
- **2026-08-22 (OpenCode opaque-server-error recovery)**: Confirmed OpenCode's generic `UnknownError` can hide unrelated upstream failures even when the same provider, model, credential, and Docker image authenticate successfully. Added error-level probe logs and one same-container retry for only the exact opaque wrapper; explicit credential diagnostics still fail immediately as unauthenticated. Verified the real Dockerized doctor path without touching active Babysit sessions. Prepared v0.32.2.
- **2026-08-22 (prompt session-close latency)**: Traced the post-resume-hint wait to the foreground auth progress UI, not Docker or detached credential cleanup. A fresh TTY reports `isPaused() === false` while `readableFlowing === null`; the UI resumed it and left the Node process alive after `/exit`. Restored non-flowing stdin across success, cancellation, and setup failures while preserving callers whose input was already flowing. Prepared v0.32.3.
- **2026-08-22 (OpenCode CI isolation)**: Build & Release #55 exposed two tests whose in-process `HOME` mutation did not redirect Bun's cached `os.homedir()`. Local OpenRouter credentials masked the issue; clean GitHub runners correctly left the model unpinned. Made the auth-profile test use an explicit model, isolated route-context assertions, and passed injectable agent context through launch argv so provider fallback remains covered without ambient files. Removed the package-only workflow trigger so a follow-up fix can retry the still-untagged release without a false version bump. Production defaults and v0.32.3 remained unchanged.
